# Codebase Cleanup Sprint - 2026-11-02
**Status**: COMPLETED
**Started**: 2026-11-02
**Lead**: Claude Opus 4.6 (cleanup swarm orchestrator)

---

## Files Modified (Safe to Edit Now)

> All cleanup tasks are complete. These files have been modified and validated.

### Console Log Removal (Agent: console-cleaner)
- `apps/league-builder/src/components/teams/TeamRosterManager.tsx` - Removed debug console.log
- `apps/league-builder/src/components/draft-room/DraftRoom.tsx` - Removed 9 debug console.log statements
- `apps/league-builder/src/components/draft-room/useDraftReliability.ts` - Removed 4 debug console.log statements
- `apps/league-builder/src/components/player-registration/registration-wizard-container.tsx` - Removed submission log
- `apps/league-builder/src/components/league-wizard/wizard-container.tsx` - Removed submission log
- `apps/league-sites/src/app/api/revalidate/route.ts` - Removed revalidation trigger log
- `apps/league-sites/src/lib/actions/registration-payments.ts` - Removed 2 checkout session logs

### Dead App Removal (Agent: dead-app-cleaner)
- `apps/league-site/` - DELETED (superseded by league-sites, only 2 commits, build failing)
- `apps/blh/` - DELETED (abandoned stub, 1 commit, outdated deps)
- `pnpm-workspace.yaml` - Verified (uses glob `apps/*` so no changes needed)

### Component Extraction (Agent: component-extractor)
- `packages/ui/src/components/standings-table.tsx` - NEW shared StandingsTable component
- `packages/ui/src/index.ts` - Added StandingsTable, StandingsTableProps, StandingsTeam exports
- `apps/league-builder/src/components/standings/StandingsTable.tsx` - Now wraps shared component
- `apps/league-sites/src/components/StandingsTable.tsx` - Now wraps shared component

---

## Task Tracker

| # | Task | Agent | Status | Notes |
|---|------|-------|--------|-------|
| 1 | Remove 16 debug console.log statements | console-cleaner | DONE | 7 files, 16 statements removed |
| 2 | Delete apps/league-site/ (dead) | dead-app-cleaner | DONE | Entire directory removed |
| 3 | Delete apps/blh/ (abandoned) | dead-app-cleaner | DONE | Entire directory removed |
| 4 | Update workspace config after deletions | dead-app-cleaner | DONE | No changes needed (glob pattern) |
| 5 | Extract StandingsTable to packages/ui | component-extractor | DONE | Shared component with admin/public variants |
| 6 | Run type-check and lint validation | team-lead | DONE | type-check PASS (9/9 packages), lint warnings pre-existing |
| 7 | Update orchestration log | team-lead | DONE | This document |

---

## Completed Work Log

### Task 1: Console Log Removal
**Agent**: console-cleaner | **Duration**: ~2 min
- Removed 16 debug `console.log` statements across 7 files
- Kept all `console.error` in catch blocks (intentional error handling)
- Files verified clean with grep post-edit

### Task 2-4: Dead App Removal
**Agent**: dead-app-cleaner | **Duration**: ~2 min
- Deleted `apps/league-site/` (118 source files, superseded by league-sites)
- Deleted `apps/blh/` (66 source files, never developed, outdated deps)
- Workspace config uses `apps/*` glob, no config updates needed
- No broken imports found across remaining codebase
- `pnpm install` ran to update lockfile

### Task 5: StandingsTable Extraction
**Agent**: component-extractor | **Duration**: ~3 min
- Created `packages/ui/src/components/standings-table.tsx` (349 lines)
  - `StandingsTeam` interface for data shape
  - `StandingsTableProps` with variant, playoffSpots, showHomeAway, renderLogo, etc.
  - Admin variant: dark theme, sortable columns, playoff indicators
  - Public variant: CSS custom property theming, search highlighting, responsive
- Both apps now import from `@hockey-life/ui` and wrap with app-specific data transforms
- Exported types: `StandingsTable`, `StandingsTableProps`, `StandingsTeam`

### Task 6: Validation
**Agent**: team-lead | **Duration**: ~3 min
- **TypeScript**: PASS - 9/9 packages, 0 errors (including modified apps)
- **ESLint league-builder**: 8 pre-existing errors (React compiler issues), 858 pre-existing warnings
- **ESLint league-sites**: 0 errors, 178 pre-existing warnings
- **ESLint mobile**: 4 pre-existing errors (require() imports in metro config - standard Expo pattern)
- No new errors or warnings introduced by cleanup changes

---

## Issues Discovered During Cleanup

1. **Pre-existing lint errors in league-builder** (8 errors):
   - React compiler "Cannot call impure function during render"
   - "Compilation Skipped: Existing memoization could not be preserved"
   - "Cannot access variable before it is declared"
   - These are not from cleanup - they predate this sprint

2. **Pre-existing lint errors in mobile** (4 errors):
   - `require()` imports in `metro.config.js` and `tailwind.config.ts`
   - Standard Expo/React Native pattern, not fixable via ESM

3. **packages/auth is placeholder**: exports `getCurrentUser`, `signIn`, `signOut` but they're stubs

---

## Post-Cleanup Suggestions

### Quick Wins (Next Sprint)
1. **Fix 8 React compiler lint errors** in league-builder (memoization/render issues)
2. **Add lint script to packages/ui** - currently has no lint config
3. **Clean up packages/auth** - either implement properly or remove placeholder functions
4. **Extract TeamLogo to packages/ui** - shared display component between league-builder and league-sites

### Medium Effort
5. **Implement real webhook tests** - `lib/stripe/__tests__/webhooks.test.ts` has 17 placeholder assertions (`expect(true).toBe(true)`)
6. **Replace manual ID inputs** - 3 TODOs for search/select components in team modals (AddPlayerModal, AddStaffModal, AddPlayerModalEnhanced)
7. **Implement QR scanner** for scorekeeper page (TODO at scorekeeper/page.tsx:231)
8. **Add shared stats display components** - multiple similar stat cards across league-sites and player-companion

### New Features/Improvements to Create
9. **Structured logging library** - Replace ad-hoc console.error with proper logging (Winston/Pino) with log levels, context, and optional Sentry/Datadog integration (TODO in webhook-logger.ts:190)
10. **CI lint gate** - Add ESLint check to CI pipeline that blocks on errors (currently only type-check runs on push)
11. **Component storybook** - packages/ui has 15+ components with no visual testing or documentation
12. **Dead code detection in CI** - Add `ts-prune` or `knip` to periodically detect unused exports
13. **Invite flow** for team management (TODO at AddPlayerModalEnhanced.tsx:261)
14. **Admin notification system** via database triggers (TODO at TeamRegistrationForm.tsx:120)

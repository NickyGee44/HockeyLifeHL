# Work Log — 2026-02-17

## Session: Notification Infrastructure

### Committed: `df2448a`
**feat: notification infrastructure — queue worker, email drafts, custom sender domains**

19 files changed, 2055 insertions, 58 deletions.

### What was done

**Part 1: Queue Worker + Send Pipeline**
- Fixed `dispatch-notifications` Edge Function (auth check, brand fixes, invoice reminder action)
- Created Vercel Cron routes: `process-notifications` (5 min), `queue-invoice-reminders` (daily 9am)
- Created `vercel.json` with cron config
- Added `CRON_SECRET` to `.env.example`

**Part 2: Email Draft + Approval Flow**
- Added 5 server actions: `saveAnnouncementDraft`, `getAnnouncementDrafts`, `deleteAnnouncementDraft`, `renderAnnouncementPreview`, `sendAnnouncementDraft`
- Rewrote `EmailComposer.tsx` to multi-step flow (compose → preview → send-options)
- Created `AnnouncementDraftList.tsx`

**Part 3: Per-League Custom Sender Domain**
- DB migration: `20260217_add_league_email_sending_domain.sql`
- `resend-domains.ts` (Resend Domains API wrapper)
- `email-sender-domain.ts` (server actions using `verifyLeagueOwnerAccess`)
- `resolveLeagueFromEmail()` threaded through `sendLeagueAnnouncement()`
- Settings page + `EmailSenderDomainSettings` component
- Navigation entry in league settings
- i18n strings (en + fr)

**Part 4: Documentation**
- Created `docs/NOTIFICATION_INFRASTRUCTURE.md`
- Updated `docs/INDEX.md`

### Bugs fixed during implementation
- JSDoc comment with `*/5 * * * *` cron expression terminated comment early — switched to `//` comments
- `owner_user_id` doesn't exist on `leagues` table — switched to `verifyLeagueOwnerAccess()` from `permissions.ts`
- Supabase `select()` with unknown columns causes `SelectQueryError` — use `select('*')` + `as any` cast
- `queue_invoice_due_reminders` RPC not in generated types — cast with `(supabase.rpc as any)()`

### Verification
- `pnpm type-check` — 9/9 pass
- `pnpm build --filter @hockey-life/league-builder` — success

### Remaining work
- Run migration on staging/production
- Run `/sync-types` after migration to remove `as any` casts
- Test end-to-end: create draft → preview → schedule → send
- Test custom domain: add domain → verify DNS → send from custom address
- Wire scheduled notification processor cron (Part 2 step 2.4 from plan)

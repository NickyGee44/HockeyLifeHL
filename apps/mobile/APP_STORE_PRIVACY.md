# App Store Privacy Notes

This file needs a fresh App Store privacy review before submission.

The previous notes in this package described an older PostHog-based mobile shell.
That is no longer the app in `apps/mobile`.

Current mobile app capabilities that must be reviewed before App Store submission:

- Supabase auth and player profile data
- Push notification token registration
- Optional location access for nearby league discovery
- Optional calendar access for adding games
- Optional camera/photo library access for profile images

Do not reuse the old PostHog privacy answers for this app.
